[pytest]
testpaths = tests
# The feature files live in their own pillar directory at the repo root, not
# beside the tests. That is deliberate: `features/` is the artefact a reviewer
# reads, and pointing pytest-bdd at it is what makes that same artefact the
# executable spec rather than a second copy of it.
bdd_features_base_dir = features
